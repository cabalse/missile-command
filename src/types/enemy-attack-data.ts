type GameLoopMetrics = {
  currentStage: number;
  currentWave: number;
  currentTick: number;
  firstWaveTick: number;
  totalMissilesFired: number;
  totalWaves: number;
};

export type { GameLoopMetrics };
