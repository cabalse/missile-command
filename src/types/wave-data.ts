type WaveData = {
  stages: number; //
  currentStageWaves: number;
  currentStage: number;
  currentWave: number;
  firstTick: number;
  lastTick: number;
  totalMissilesFired: number;
};

export type { WaveData };
